<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Warenkorb</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="layout.css">
</head>
<body>
        <div class="topnav">
                <a href="/">Home</a>
                <a href="/produktlist">Produkte</a>
                <%if (login === false ){%>
                    <a href="/anmelden">Anmelden</a>
                    <a href="/kontoerstellen">Konto erstellen</a>
                <%}else{%>
                    <a href="/abmelden">Abmelden</a>
                <%}%>
                <a class="active" href="/warenkorb">
                    <img src="images/shopping-cart.png" alt="Warenkorb" style="width:20px;height:20px;">
                </a>
                <br>
        <form action="/suche" method="POST">
            <input type="text" name="suche">
            <button type="submit">Suchen</button>
            </form>
         </div>
        
        <%if (produkte.length === 0 ){%>
            <p>Es befinden sich keine Produkte in deinem Warenkorb.</p>
        <%}else{%>
            <p>Die folgenden Produkte befinden sich in Warenkorb:</p>
            <% for (let i = 0; i < produkte.length; i++) { %>
                <p><%= produkte[i].name%>: <b><%= produkte[i].preis%> EUR</b></p> 
                <a href="/produkt?id=<%= produkte[i].id %>">
                    <img src="images/<%= produkte[i].bild_datei %>" alt="<%= produkte[i].bild_datei %>" style="width:200px;height:200px;">
            </a>
            <% } %>
        <%}%>
        <br>
        <a href="/produktlist">Weiter einkaufen</a><br>

        <%if (produkte.length !== 0 ){%>
            <%if (login === false ){%>
                <a href="/anmelden">Anmelden um zu bestellen</a>
            <%}else{%>
                <a href="/bestellen">Bestellen jetzt!</a>
            <%}%>
        <%}%>
    </body>
</html>
